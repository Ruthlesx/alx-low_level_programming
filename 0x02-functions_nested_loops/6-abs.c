#include "main.h"

/**
 * _abs- computer the absolute value of an integer
 * @n the int too check
 * Return. the absolute value of int 
 */

int _abs(int n)
{
	if (n >= 0)
	{
		return(n);
	}
	return(-n);
}
